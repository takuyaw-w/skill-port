{
  "name": "@skill-port/admin-web",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --host 0.0.0.0 --port 3001",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview --host 0.0.0.0 --port 3001",
    "typecheck": "nuxt typecheck",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@skill-port/contracts": "workspace:*",
    "@skill-port/shared": "workspace:*",
    "nuxt": "catalog:",
    "vue": "catalog:",
    "vue-router": "catalog:"
  }
}
